[build]
builder = "NIXPACKS"

[deploy]
startCommand = "gunicorn wsgi:app --bind 0.0.0.0:$PORT --access-logfile -"
healthcheckPath = "/health"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
